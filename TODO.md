# TODO

- Move/standardize OneDrive folder structure to `Photos/dev-storage/` and `Photos/dev-storage/thumb/`.
- Regenerate dev-storage images locally (default + selective thumbs) and upload to OneDrive.
- Decide and implement Vercel Blob upload flow for dev-storage outputs.
- Switch the app to use Vercel Blob URLs for dev-storage assets.
- Verify list/card thumbnails and detail views after the URL switch.
- Decide GIF/MP4 optimization strategy (convert/keep, size/quality targets).
- Update documentation for the new asset flow.
