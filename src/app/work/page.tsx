import StaticContentPage from '@/components/StaticContentPage';

export default function WorkPage() {
  // Flow 2: /work is introduced as a bounded realignment surface with baseline content.
  return <StaticContentPage slug="work" />;
}
