import StaticContentPage from '@/components/StaticContentPage';

export default function WritingPage() {
  // Flow 2: /writing is introduced as a bounded realignment surface with baseline content.
  return <StaticContentPage slug="writing" />;
}
