import StaticContentPage from '@/components/StaticContentPage';

export default function ResumePage() {
  // Flow 2: /resume is introduced as a bounded realignment surface with baseline content.
  return <StaticContentPage slug="resume" />;
}
