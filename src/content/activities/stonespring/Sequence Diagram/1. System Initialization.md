@startuml DoraSystem_Initialization

participant main.py
participant "views.GUI" as GUI
participant "network.tcp_server" as TCP

main.py -> TCP : start_server() (thread로 실행)
main.py -> GUI : PyQt GUI 실행

@enduml
